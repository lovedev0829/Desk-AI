import { paths } from 'src/routes/paths';

export const SERVER_URL = 'https://salad-transcription.onrender.com';

// ROOT PATH AFTER LOGIN SUCCESSFUL
export const PATH_AFTER_LOGIN = paths.dashboard.root; // as '/dashboard'
 